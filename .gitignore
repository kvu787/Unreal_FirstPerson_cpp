# Visual Studio uses this folder to store user-specific settings.
/.vs/

/Binaries/
/DerivedDataCache/
/Intermediate/
/Saved/
/*.sln

# These files are generated by Unreal.
# They contain absolute paths that are specific to my machine, so I don't think they should be committed.
/Build/Windows/FileOpenOrder/CookerOpenOrder.log
/Build/Windows/FileOpenOrder/EditorOpenOrder.log
